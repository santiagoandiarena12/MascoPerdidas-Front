export const environment = {
  production: true,
  // Temporalmente apuntamos a localhost para que puedas probar con tu Java localmente.
  // Cuando vayas a desplegar (subir a internet), cambias esto de vuelta a tu dominio real.
  apiUrl: 'http://localhost:8080/api' 
};